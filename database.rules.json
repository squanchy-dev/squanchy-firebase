{
  "rules": {
    "data": {
      ".read": "auth != null",
      ".write": false
    },
    "user": {
      "$user_id": {
        ".write": "$user_id === auth.uid",
        ".read": "$user_id === auth.uid"
      }
    }
  }
}
